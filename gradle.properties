# Enable AndroidX support (required for modern AndroidX dependencies)
android.useAndroidX=true

# Prevent R class from containing resource IDs of transitive dependencies (improves build speed)
android.nonTransitiveRClass=true

# Gradle daemon JVM settings
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
